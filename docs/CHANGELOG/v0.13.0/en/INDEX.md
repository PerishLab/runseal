# Runseal v0.13.0

Runseal adopts the shared binary delivery closure. `plumb.toml` is its only
product release declaration; build matrices, archives, managers, sealed
records, verification, activation, smoke, and tags are owned by Plumb and
Actions.

Canonical stable is the default installation anchor. Exact non-stable
candidates remain isolated validation tools.
