[package]
name = "runseal"
version = "0.14.1"
edition = "2024"

[dependencies]
plumb = { version = "0", registry = "perish" }
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
path-absolutize = "3.1"

[dev-dependencies]
tempfile = "3.12"
