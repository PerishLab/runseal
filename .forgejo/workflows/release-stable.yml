name: release-stable

on:
  workflow_dispatch:
    inputs:
      version_override:
        description: "Optional stable version override, for example v0.10.0"
        required: false
        default: ""
        type: string

jobs:
  release:
    runs-on: docker
    container:
      image: mirror.perish.lan/ci/deno
    env:
      CI_COMMIT: ${{ github.sha }}
      CI_REPOSITORY: ${{ github.repository }}
      CI_RUN_ATTEMPT: ${{ github.run_attempt }}
      CI_RUN_ID: ${{ github.run_id }}
      CI_WORKFLOW: release-stable
      RUNSEAL_RELEASES_PUBLIC_URL: ${{ vars.RUNSEAL_RELEASES_PUBLIC_URL }}
      RUNSEAL_RELEASES_S3_AK: ${{ secrets.RUNSEAL_RELEASES_S3_AK }}
      RUNSEAL_RELEASES_S3_BUCKET: ${{ secrets.RUNSEAL_RELEASES_S3_BUCKET }}
      RUNSEAL_RELEASES_S3_SK: ${{ secrets.RUNSEAL_RELEASES_S3_SK }}
      RUNSEAL_RELEASES_S3_URL: ${{ secrets.RUNSEAL_RELEASES_S3_URL }}
      R2_ACCESS_PROBE_NAME: release-stable
      RELEASE_CHANNEL: stable
      SMOKE_LATEST: "1"
      STABLE_VERSION_OVERRIDE: ${{ inputs.version_override }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install pinned Negentropy
        run: |
          set -eu
          version=$(cat .runseal/negentropy.version)
          curl -fsSL https://releases.negentropy.perish.uk/manage.sh -o "$RUNNER_TEMP/manage-negentropy.sh"
          sh "$RUNNER_TEMP/manage-negentropy.sh" install --channel beta --version "$version" --bin-dir "$HOME/.local/bin"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Validate R2 access
        run: bash .forgejo/scripts/release/r2/check.sh

      - name: Resolve stable metadata
        id: stable
        run: deno run --allow-env --allow-read --allow-net --allow-write="$GITHUB_OUTPUT" .forgejo/scripts/release/metadata/stable.ts

      - name: Guard
        run: |
          set -eu
          export PATH="$HOME/.local/bin:$PWD/target/debug:$PATH"
          cargo build --quiet --locked -p runseal
          target/debug/runseal :guard

      - name: Package
        env:
          RELEASE_VERSION: ${{ steps.stable.outputs.release_version }}
          TARGET: x86_64-unknown-linux-gnu
        run: sh .forgejo/scripts/release/assets/package.sh "$RELEASE_VERSION"

      - name: Publish
        env:
          BASE_VERSION: ${{ steps.stable.outputs.base_version }}
          RELEASE_ROOT: dist/${{ steps.stable.outputs.release_version }}
          RELEASE_VERSION: ${{ steps.stable.outputs.release_version }}
          STATE_SOURCE: ${{ steps.stable.outputs.state_source }}
        run: |
          set -eu
          export PATH="$HOME/.local/bin:$PWD/target/debug:$PATH"
          export GUARD_VERSION_HASH="$(target/debug/runseal :guard version-hash)"
          sh .forgejo/scripts/release/assets/checksums.sh "$RELEASE_VERSION" "$RELEASE_ROOT"
          sh .forgejo/scripts/release/assets/verify.sh accept "$RELEASE_VERSION" "$RELEASE_ROOT"
          sh .forgejo/scripts/release/assets/verify.sh verify "$RELEASE_VERSION" "$RELEASE_ROOT"
          bash .forgejo/scripts/release/r2/publish.sh

      - name: Verify R2 publish
        env:
          R2_METADATA_URL: ${{ vars.RUNSEAL_RELEASES_PUBLIC_URL }}/stable/latest/metadata.json
          RELEASE_VERSION: ${{ steps.stable.outputs.release_version }}
        run: bash .forgejo/scripts/release/r2/verify.sh

      - name: Tag release
        env:
          RELEASE_VERSION: ${{ steps.stable.outputs.release_version }}
        run: |
          set -eu
          git config user.name "forgejo-actions"
          git config user.email "actions@git.perish.top"
          if ! git rev-parse --verify --quiet "refs/tags/$RELEASE_VERSION" >/dev/null; then
            git tag -a "$RELEASE_VERSION" -m "$RELEASE_VERSION"
          fi
          git push origin "refs/tags/$RELEASE_VERSION"

      - name: Publish summary
        env:
          BASE_VERSION: ${{ steps.stable.outputs.base_version }}
          R2_METADATA_URL: ${{ vars.RUNSEAL_RELEASES_PUBLIC_URL }}/stable/latest/metadata.json
          R2_VERSION_METADATA_URL: ${{ vars.RUNSEAL_RELEASES_PUBLIC_URL }}/stable/versions/${{ steps.stable.outputs.release_version }}/metadata.json
          R2_VERSION_PREFIX: stable/versions/${{ steps.stable.outputs.release_version }}
          RELEASE_VERSION: ${{ steps.stable.outputs.release_version }}
          STATE_SOURCE: ${{ steps.stable.outputs.state_source }}
        run: bash .forgejo/scripts/release/r2/summary.sh

      - name: Smoke install
        run: sh .forgejo/scripts/release/smoke/smoke.sh "${{ steps.stable.outputs.release_version }}" stable
